{
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5434;Database=predictive_maintenance_db;Username=fv_admin;Password=${POSTGRES_PASSWORD}"
  },
  "Redis": {
    "ConnectionString": "localhost:6380,password=${REDIS_PASSWORD}"
  },
  "Kafka": {
    "BootstrapServers": "localhost:9092"
  },
  "OpenTelemetry": {
    "Endpoint": "http://localhost:4317"
  },
  "Jwt": {
    "SigningKey": "${JWT_SIGNING_KEY}"
  }
}
